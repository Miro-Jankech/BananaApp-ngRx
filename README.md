ORDER:


state folder:
 -- > banana.state.ts with proporties and initialState empty
  --> banana.actions.ts with action(s)
    --> banana.reducer.ts with case switching these actions and default state 
      --> WIRE IT UP --> index.ts exporting everyting
        

        --> GLOBAL settings in app.config.ts